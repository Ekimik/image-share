<div class="panel panel-default">
    <div class="panel-heading" role="tab" id="heading-{$form->getName()}">
        <h4 class="panel-title">
            <a role="button" data-toggle="collapse" data-parent="#accordion-share-options" href="#collapse-{$form->getName()}"
               aria-expanded="false" aria-controls="collapse-{$form->getName()}" class="btn btn-info">
                {if $aboutBlockName === 'shareForm'}
                    Nemám účet na Facebooku
                {/if}
            </a>
        </h4>
    </div>

    <div id="collapse-{$form->getName()}" role="tabpanel" aria-labelledby="heading-{$form->getName()}"
         class="panel-collapse collapse {if $form->hasErrors()}in{/if}">

        <div class="panel-body">
            {if $aboutBlockName === 'shareForm'}
                <p class="share-option-about">
                    Pokud chcete pouze přispět fotkou či videem z Vaší cesty, použijte formulář níže. V tomto
                    případě se Vámi zaslaný obsah pouze přidá do našeho úložiště, ze kterého poté vznikne oficiální
                    galerie. Tu po té budete moci najít na stránkách
                    <a href="http://www.kosetice.cz/" target="_blank">obce Košetice</a>.
                </p>
            {/if}

            {control $form->getName()}

            <div class="text-left">
                <div class="inline-block-label label label-info">
                    Nahráním dáváte souhlas se zveřejněním souborů dle uvážení pořadatelů.
                </div>
            </div>
        </div>
    </div>
</div>

{* hack *}
<script type="text/javascript">
    {if $form->hasErrors()}
        window.location.hash = '#upload';
    {/if}
</script>